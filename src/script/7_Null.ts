let val7: null = null;

// let count: number = 1;
// count = null; // tsconfig.json's strictNullChecks is true, it catch error!

export default function() {
	console.log('val7', val7);
	// console.log('count', count);
};